const userModel = require('./userModel');
const tokenBlacklistModel = require('./tokenBlacklistModel');
const origamiModel = require('./origamiModel');

module.exports = {
    userModel,
    tokenBlacklistModel,
    origamiModel
}